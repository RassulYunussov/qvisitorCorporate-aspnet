@model IEnumerable<qvisitorCorp.Models.qvOrder>

@{

    ViewData["Title"] = "Orders";
}
<div id="page-wrapper">
    <div class="row">
        <div class="col-lg-12">
            <h1 class="page-header">Заявки</h1>
        </div>
        <!-- /.col-lg-12 -->
    </div>
    <ol class="breadcrumb">
        <li class="active">
            Заявки
        </li>
    </ol>
    
    <div class="panel panel-default">
        <div class="panel-heading">
            <i class="glyphicon glyphicon-list-alt"></i>
            Список заявок
        </div>

        <div class="panel-body">
            <div class="dataTable_wrapper">
                <!--
                <table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CloseTime)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EndDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.OpenTime)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.StartDate)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CloseTime)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EndDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.OpenTime)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.StartDate)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>    
                -->

                <table class="table table-striped table-bordered table-hover dataTable no-footer" id="dataTables-example" role="grid" aria-describedby="dataTables-example_info">
                    <thead>
                        <tr>
                            <th rowspan="2" style="text-align:center; vertical-align:middle; width:50px;">#</th>
                            <th>
                                @Html.DisplayNameFor(model => model.CloseTime)
                            </th>
                            <th>
                                @Html.DisplayNameFor(model => model.EndDate)
                            </th>
                            <th>
                                @Html.DisplayNameFor(model => model.OpenTime)
                            </th>
                            <th>
                                @Html.DisplayNameFor(model => model.StartDate)
                            </th>
                            <th>Действия</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>
                                    @Html.DisplayFor(modelItem => item.Id)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.CloseTime)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.EndDate)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.OpenTime)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.StartDate)
                                </td>
                                <td>
                                    <a style="color:#333;" asp-action="OrdersDetails" asp-route-id="@item.Id"><button class="btn" style="margin:1px;">Просмотр</button></a>
                                    <a style="color:#333;" asp-action="OrdersEdit" asp-route-id="@item.Id"><button class="btn" style="margin:1px;">Изменить</button></a>
                                    <a style="color:#333;" asp-action="OrdersDelete" asp-route-id="@item.Id"><button class="btn" style="margin:1px;">Удалить</button></a>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
            <div class="well">
                <a style="color:#333;" asp-controller="Cabinet" asp-action="OrdersCreate">
                    <button class="btn" style="margin-right:0;">Добавить</button>
                </a>

                <a style="color:#333;" asp-action="OrdersDetails" asp-route-id="1"><button class="btn" style="margin:1px;">Просмотр</button></a>
                <a style="color:#333;" asp-action="OrdersEdit" asp-route-id="1"><button class="btn" style="margin:1px;">Изменить</button></a>
                <a style="color:#333;" asp-action="OrdersDelete" asp-route-id="1"><button class="btn" style="margin:1px;">Удалить</button></a>

            </div>
        </div>
    </div>
</div>
<!-- jQuery -->
<script src="../bower_components/jquery/dist/jquery.min.js"></script>

<!-- Bootstrap Core JavaScript -->
<script src="~/bower_components/bootstrap/dist/js/bootstrap.min.js"></script>

<!-- Metis Menu Plugin JavaScript -->
<script src="~/bower_components/metisMenu/dist/metisMenu.min.js"></script>

<!-- DataTables JavaScript -->
<script src="~/bower_components/datatables/media/js/jquery.dataTables.min.js"></script>
<script src="~/bower_components/datatables-plugins/integration/bootstrap/3/dataTables.bootstrap.min.js"></script>

<!-- Custom Theme JavaScript -->
<script src="~/js/sb-admin-2.js"></script>

<!-- Page-Level Demo Scripts - Tables - Use for reference -->
<script>
    $(document).ready(function() {
        $('#dataTables-example').DataTable({
                responsive: true
        });
    });
</script>
